import React from 'react';

/*
This is the Header Component which will show the name "Elf Computing" at the top of the webpage
    -It does not take in any props and is only referenced once on the App Component
*/
function ElfHeader() {
    return (
        <header>
            <h1>ELF Computing | Laptops</h1>
        </header>
    );
}

export default ElfHeader;
